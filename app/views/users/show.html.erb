 <h1>簡易労働時間計算</h1>
 <%= link_to '戻る', root_path %>
  <br>
■１日間の法定労働時間　　　max:8時間
 <br>
■１週間の法定労働時間　　　max:40時間
<br>
■１ヶ月の法定時間外労働　　max:45時間

 <div class=alltime>
   <form class="time_kanri">

    <div class=keisan>
    <br>
        <b>月曜日</b>
        <div><input  type="number" class="box" name="a" placeholder="半角数字のみ">時間</div>
        <b>火曜日</b>
        <div><input type="number" class="box" name="b" placeholder="半角数字のみ">時間</div>
        <b>水曜日</b>
        <div><input type="number" class="box" name="c" placeholder="半角数字のみ">時間</div>
        <b>木曜日</b>
        <div><input type="number" class="box" name="d" placeholder="半角数字のみ">時間</div>
        <b>金曜日</b>
        <div><input type="number" class="box" name="e" placeholder="半角数字のみ">時間</div>

        <p><input type="button" class="calc-btn" onclick="calc(a.value, b.value,c.value,d.value,e.value);" value="計算"></p>
        <b>総労働時間(週)</b>
        <div><input id="weekall" class="box" type="text" readonly="">時間</div>
        <b>残り法定労働時間(週)</b>
        <div><input id="weekqua" class="box" type="text"  readonly="">時間</div>

       </div>
       </form>
       <script>

       function calc(a, b,c,d,e) {
       
       
        const weekall_value = document.getElementById('weekall').value 
        = Math.round (parseInt(a) + parseInt(b)+ parseInt(c)+ parseInt(d)+ parseInt(e) ) ;
        
        const weekqua_value = document.getElementById('weekqua').value
        = Math.round  (40 - (parseInt(a) + parseInt(b)+ parseInt(c)+ parseInt(d)+ parseInt(e) )) ;
    
       }
       </script>
       

   <form class="time_kanri">

    <div class=keisan>
    <br>
        <b>労働日数(月)</b>
        <div><input  type="number" class="box" name="f" placeholder="半角数字のみ">日　</div>
        <b>平均労働時間(日)</b>
        <div><input type="number" class="box" name="g" placeholder="半角数字のみ">時間</div>
        <b>(所定・法定)労働時間(日)</b>
        <div><input type="number" class="box" name="h" placeholder="半角数字のみ">時間</div>
    <br>
    <br>
    <br>
    <br>

        <p><input type="button" class="cal-btn" onclick="cal(f.value, g.value, h.value);" value="計算"></p>
        <b>残り法定時間外労働(月)</b>
        <div><input id="monthall" class="box" type="text" readonly="">時間</div>
        <b>残業時間(月)</b>
        <div><input id="monthqua" class="box" type="text" readonly="">時間</div>
       </div>
       </form>

       <script>

       function cal(f,g,h) {
     
        const monthall_value = document.getElementById('monthall').value 
        = Math.round (45  -  ((parseInt(f) * parseInt(g)) - (parseInt(f)*parseInt(h))) );
       
        const monthqua_value = document.getElementById('monthqua').value 
        = Math.round  ((parseInt(f) * parseInt(g)) - (parseInt(f)*parseInt(h)));
       }
       </script>


       <textarea  placeholder="メモ帳" class="large-field"></textarea>
    </div>
 </div>


